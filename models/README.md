This directory contains trained ML models and checkpoints.
The models themselves (*.pt, *.pth, *.h5, etc.) are excluded from version control or tracked via Git LFS depending on project configuration. This directory also contains tracked model registry definitions and metadata.
